#include "Animal.h"
#include "TimeManager.h"
#include "windows.h"
#include "Cat.h"

int main() {
    TimeManager systemTime;
    // Cat *cat = new Cat( "Mimi" );
    // for ( int i = 0; i < 100; i++ ) {
    //     systemTime.updateTime();
    //     std::array< int, 2 >currentTime = systemTime.getTime();
    //     std::cout << "day: " << currentTime[0] << " hour: " << currentTime[1] << '\n';
    //     Sleep(3000);
    // }
    return 0;
}